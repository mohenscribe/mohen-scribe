{
  "name": "mohen-scribe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Audio and video transcription MCP plugin for ChatGPT and Codex.",
  "scripts": {
    "start": "node server.js",
    "start:stdio": "node stdio-server.js",
    "check": "node --check server.js && node --check stdio-server.js && node --check src/mcp.js && node --check src/transcribe.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "formidable": "^3.5.4",
    "openai": "^6.0.0",
    "zod": "^3.25.76"
  }
}
